---
const { label, id, name, required } = Astro.props;

interface Props {
  label: string;
  name: string;
  id: string;
  required?: boolean;
}
---

<div>
  <label for={id} class="sr-only">{label}</label>
  <textarea
    id={id}
    name={name}
    rows="4"
    class="block w-full rounded-lg border border-neutral-600 bg-neutral-700/30 px-4 py-3 text-sm text-neutral-700 placeholder:text-neutral-500 focus:border-neutral-200 focus:ring-3 focus:ring-neutral-400 focus:outline-hidden disabled:pointer-events-none disabled:opacity-50"
    placeholder={label}
    required={required}></textarea>
</div>
