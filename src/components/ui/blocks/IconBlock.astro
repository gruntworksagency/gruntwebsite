---
// Get heading and content from Astro props
const { heading, content, link, linkText } = Astro.props;
// Define TypeScript interface for props
interface Props {
  heading?: string;
  content?: string;
  link?: string;
  linkText?: string;
}
// Define classes for heading and content
const headingClasses = "text-balance text-lg font-bold text-neutral-300";
const contentClasses = "mt-1 text-pretty text-neutral-500";
const linkClasses = "text-[#1a5cc7] underline hover:text-[#092e69] font-bold";
---

<!-- The root container that arranges your slot and the heading/content -->
<div class="flex gap-x-5">
  <!-- Slot to allow for extensibility of the component -->
  <slot />
  <div class="grow">
    <!-- Heading of the section -->
    <h3 class={headingClasses}>
      {heading}
    </h3>
    <!-- Content text of the section -->
    <p class={contentClasses}>
      {content}
      {
        link && linkText && (
          <div class="mt-2">
            <a href={link} class={linkClasses}>
              {linkText}
            </a>
          </div>
        )
      }
    </p>
  </div>
</div>
